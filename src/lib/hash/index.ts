/** Canonicalize + SHA-256 menu hashing. Implement for real publish/verify. */
export async function hashCanonicalMenu(_menu: unknown): Promise<string> {
  throw new Error("lib/hash not implemented — use fixtures for now");
}
